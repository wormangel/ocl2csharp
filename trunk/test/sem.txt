context Conta inv:
pontos = 5 + 6

--context ProgramaFidelidade inv:
--socio.conta > 0

context Cliente inv:
cartoes->size() > 1

context ProgramaFidelidade::cadastrar(c:Cliente):boolean
pre: 3 > 4

--context Cliente inv:
--cartoes->size() > 1


-- result, select, self, path (com metodo) no context, pontos = 5+6, pre e pos, inv (BOOLEAN!!)



--
--context Cliente
--inv: blablabla < 3

--context ParceiroPrograma
--inv c: true and 3

--  propriedade que nao existe
--context Conta
--inv c: xxx and false

-- propriedade que nao eh boolean
--context Conta
--inv c: pontos and false

--propriedade que eh boolean
context Conta
inv c: estaVazia and false

-- operacao em colecao
context Cliente
inv c: cartoes->size()

context Cliente
inv teste: cartoes->exists(c)

context Cliente
inv: cartoes->exists(idade : integer | idade < 3)

context Cliente
inv teste: cartoes->forAll(3 < 4)

context Cliente
inv c: cartoes->includes(x)